# 2026-06-14 剩余简单候选离线筛选

> 本文只基于 `docs/T20_OFFICIAL_COMMANDS.md`、`docs/T20_COMMANDS.md` §1.1/§1.2、以及 `src/t20_mcp/tools/tangent.py` 末尾 `_GENERATORS` 做离线归并。未做任何真机、AutoCAD、T20、测试、`.venv` 或 `uv` 操作。
>
> 口径：官方总表共 454 行、451 个大小写无关命令名；按 §1.1 已封装/已禁用 dry-run 路径与 §1.2 已验证存在/已探测/已拒绝命令名大小写无关排除后，剩余 407 行、405 个大小写无关命令名。官方表中的重复项 `Tclip`、`TQuickDim` 按官方行保留。
>
> 预测分类统计：纯造点序列 22 行；需选已有对象 170 行；必弹对话框 48 行；修改已有实体 82 行；未知 85 行。所有分类均为（推断），不是实机结论。

## 1. 剩余未封装且未探测命令全表

| 官方行 | 中文命令名 | 命令名 | 注册 | 预测交互类型 | 判据依据 |
|---:|---|---|---|---|---|
| L11 | 绘制轴网 | `TArcAxis` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“绘制轴网”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L12 | 墙生轴网 | `TWall2Axis` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“墙生轴网”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L13 | 轴网标注 | `TMultAxisDim` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“轴网标注”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L14 | 单轴标注 | `TSingleAxisDim` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“单轴标注”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L15 | 重排轴号 | `TReAxisNo` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“重排轴号”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L16 | 添补轴号 | `TAddLabel` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“添补轴号”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L17 | 删除轴号 | `TDelLabel` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“删除轴号”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L18 | 添加轴线 | `TInsAxis` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“添加轴线”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L19 | 轴线裁剪 | `TClipAxis` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“轴线裁剪”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L20 | 轴网合并 | `TMergeAxis` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“轴网合并”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L21 | 轴改线型 | `TAxisDote` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“轴改线型”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L22 | 一轴多号 | `TMutiLabel` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“一轴多号”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L23 | 轴号隐现 | `TShowLabel` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“轴号隐现”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L24 | 主附转换 | `TChAxisNo` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“主附转换”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L26 | 角柱 | `TCornColu` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“角柱”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L27 | 构造柱 | `TFortiColu` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“构造柱”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L28 | 异形柱 | `TPolyColu` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“异形柱”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L29 | 柱齐墙边 | `TAlignColu` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“柱齐墙边”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L31 | 等分加墙 | `TDivWall` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“等分加墙”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L32 | 墙体分段 | `TPartwall` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“墙体分段”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L34 | 倒墙角 | `TFillet` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“倒墙角”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L35 | 倒斜角 | `TChamfer` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“倒斜角”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L36 | 修墙角 | `TFixWall` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“修墙角”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L37 | 基线对齐 | `TAdjWallBase` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“基线对齐”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L38 | 边线对齐 | `TAlignWall` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“边线对齐”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L39 | 净距偏移 | `TOffset` | （总表已注册✓） | 必弹对话框（推断） | （推断，不是真机结论）中文名“净距偏移”偏库、管理、设置、报表、导入导出、批量或参数配置语义，预测更像先进入对话框/面板。 |
| L40 | 改墙厚 | `TWallThick` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“改墙厚”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L41 | 改外墙厚 | `TExtThick` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“改外墙厚”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L42 | 墙柱保温 | `TAddInsulate` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“墙柱保温”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L43 | 改高度 | `TChHeight` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“改高度”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L44 | 改外墙高 | `TChEWallHeight` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“改外墙高”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L45 | 异型立面 | `TShapeWall` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“异型立面”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L46 | 矩形立面 | `TDelWallShape` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“矩形立面”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L47 | 墙面UCS | `TUcsWall` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“墙面UCS”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L48 | 识别内外 | `TMarkWall` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“识别内外”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L49 | 指定内墙 | `TMarkIntWall` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“指定内墙”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L50 | 指定外墙 | `TMarkExtWall` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“指定外墙”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L51 | 加亮外墙 | `THLExtWall` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“加亮外墙”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L53 | 墙齐屋顶 | `TWallAlignroof` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“墙齐屋顶”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L54 | 更新造型 | `TAUpdPatch` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“更新造型”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L55 | 平行生线 | `TWall2Curve` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“平行生线”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L57 | 带形窗 | `TBanWin` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“带形窗”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L58 | 组合门窗 | `TGroupOpening` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“组合门窗”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L59 | 转角窗 | `TCornerWin` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“转角窗”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L60 | 异形洞 | `TPolyHole` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“异形洞”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L61 | 内外翻转 | `TMirWinIO` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“内外翻转”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L62 | 左右翻转 | `TMirWinLR` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“左右翻转”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L63 | 门窗套 | `TOpSlot` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“门窗套”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L64 | 门口线 | `TDoorLine` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“门口线”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L65 | 编号复位 | `TResetLabPos` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“编号复位”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L66 | 编号后缀 | `TLabelFix` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“编号后缀”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L67 | 门窗检查 | `TValidOp` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“门窗检查”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L68 | 门窗表 | `TStatOp` | （总表已注册✓） | 必弹对话框（推断） | （推断，不是真机结论）中文名“门窗表”偏库、管理、设置、报表、导入导出、批量或参数配置语义，预测更像先进入对话框/面板。 |
| L69 | 门窗总表 | `TPrjOP` | （总表已注册✓） | 必弹对话框（推断） | （推断，不是真机结论）中文名“门窗总表”偏库、管理、设置、报表、导入导出、批量或参数配置语义，预测更像先进入对话框/面板。 |
| L70 | 加装饰套 | `TOpeningSlot` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“加装饰套”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L71 | 窗棂展开 | `TExpWinFrame` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“窗棂展开”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L72 | 窗棂映射 | `TMapWinFrame` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“窗棂映射”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L73 | 门窗原型 | `TOpTem` | （总表已注册✓） | 必弹对话框（推断） | （推断，不是真机结论）中文名“门窗原型”偏库、管理、设置、报表、导入导出、批量或参数配置语义，预测更像先进入对话框/面板。 |
| L74 | 门窗入库 | `TWin2Lib` | （总表已注册✓） | 必弹对话框（推断） | （推断，不是真机结论）中文名“门窗入库”偏库、管理、设置、报表、导入导出、批量或参数配置语义，预测更像先进入对话框/面板。 |
| L75 | 查询面积 | `TSpArea` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“查询面积”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L76 | 套内面积 | `TApartArea` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“套内面积”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L77 | 公摊面积 | `TShareArea` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“公摊面积”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L78 | 面积累加 | `TPlusText` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“面积累加”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L80 | 房间轮廓 | `TSpOutline` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“房间轮廓”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L81 | 房间排序 | `TSortSpace` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“房间排序”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L82 | 面积统计 | `TCountArea` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“面积统计”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L83 | 住宅面积 | `TProjectArea` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“住宅面积”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L84 | 加踢脚线 | `TKickBoard` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“加踢脚线”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L85 | 布置洁具 | `TSan` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“布置洁具”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L86 | 布置隔断 | `TApart` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“布置隔断”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L87 | 布置隔板 | `TClap` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“布置隔板”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L88 | 搜屋顶线 | `TRoflna` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“搜屋顶线”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L89 | 任意坡顶 | `TSlopeRoof` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“任意坡顶”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L90 | 人字坡顶 | `TDualSlopeRoof` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“人字坡顶”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L93 | 加老虎窗 | `TDormer` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“加老虎窗”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L94 | 加雨水管 | `TStrm` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“加雨水管”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L97 | 任意梯段 | `TCStair` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“任意梯段”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L98 | 添加扶手 | `THandRail` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“添加扶手”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L99 | 连接扶手 | `TLinkHand` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“连接扶手”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L100 | 双跑楼梯 | `TRStair` | （总表已注册✓） | 纯造点序列（推断） | （推断，不是真机结论）中文名“双跑楼梯”是绘制/插入/布置类造物语义，预测可由位置点、两点或少量点列生成新实体；仍需确认是否有参数面板。 |
| L101 | 多跑楼梯 | `TMultiStair` | （总表已注册✓） | 纯造点序列（推断） | （推断，不是真机结论）中文名“多跑楼梯”是绘制/插入/布置类造物语义，预测可由位置点、两点或少量点列生成新实体；仍需确认是否有参数面板。 |
| L103 | 自动扶梯 | `tdrawautostair` | （总表已注册✓） | 纯造点序列（推断） | （推断，不是真机结论）中文名“自动扶梯”是绘制/插入/布置类造物语义，预测可由位置点、两点或少量点列生成新实体；仍需确认是否有参数面板。 |
| L104 | 双分平行 | `TDrawParallelStair` | （总表已注册✓） | 纯造点序列（推断） | （推断，不是真机结论）中文名“双分平行”是绘制/插入/布置类造物语义，预测可由位置点、两点或少量点列生成新实体；仍需确认是否有参数面板。 |
| L105 | 双分转角 | `TDrawCornerStair` | （总表已注册✓） | 纯造点序列（推断） | （推断，不是真机结论）中文名“双分转角”是绘制/插入/布置类造物语义，预测可由位置点、两点或少量点列生成新实体；仍需确认是否有参数面板。 |
| L106 | 双分三跑 | `TDrawDoubleMulStair` | （总表已注册✓） | 纯造点序列（推断） | （推断，不是真机结论）中文名“双分三跑”是绘制/插入/布置类造物语义，预测可由位置点、两点或少量点列生成新实体；仍需确认是否有参数面板。 |
| L107 | 交叉楼梯 | `TDrawScissorsStair` | （总表已注册✓） | 纯造点序列（推断） | （推断，不是真机结论）中文名“交叉楼梯”是绘制/插入/布置类造物语义，预测可由位置点、两点或少量点列生成新实体；仍需确认是否有参数面板。 |
| L108 | 剪刀楼梯 | `TDrawCrossStair` | （总表已注册✓） | 纯造点序列（推断） | （推断，不是真机结论）中文名“剪刀楼梯”是绘制/插入/布置类造物语义，预测可由位置点、两点或少量点列生成新实体；仍需确认是否有参数面板。 |
| L109 | 三角楼梯 | `TDrawTriangleStair` | （总表已注册✓） | 纯造点序列（推断） | （推断，不是真机结论）中文名“三角楼梯”是绘制/插入/布置类造物语义，预测可由位置点、两点或少量点列生成新实体；仍需确认是否有参数面板。 |
| L113 | 散水 | `TOutlna` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“散水”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L116 | 竖板 | `TVertSlab` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“竖板”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L117 | 路径曲面 | `TCurveMesh` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“路径曲面”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L118 | 变截面体 | `TPolyMesh` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“变截面体”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L119 | 等高建模 | `TSlope` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“等高建模”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L120 | 栏杆库 | `TRailLib` | （总表已注册✓） | 必弹对话框（推断） | （推断，不是真机结论）中文名“栏杆库”偏库、管理、设置、报表、导入导出、批量或参数配置语义，预测更像先进入对话框/面板。 |
| L121 | 路径排列 | `TRail` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“路径排列”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L122 | 三维网架 | `TNetShelf` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“三维网架”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L123 | 线转面 | `TLine2Face` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“线转面”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L124 | 实体转面 | `TSol2Face` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“实体转面”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L125 | 面片合成 | `TMergeFace` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“面片合成”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L126 | 隐去边线 | `THideEdge` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“隐去边线”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L127 | 三维切割 | `TClip3D` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“三维切割”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L128 | 厚线变面 | `TCurve2Face` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“厚线变面”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L129 | 线面加厚 | `TSF3D` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“线面加厚”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L130 | 基本形体 | `TSolBox` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“基本形体”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L131 | 截面拉伸 | `TSolExtrude` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“截面拉伸”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L132 | 截面旋转 | `TSolRevolve` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“截面旋转”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L133 | 截面放样 | `TSolSweep` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“截面放样”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L134 | 布尔并集 | `TSolUnion` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“布尔并集”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L135 | 布尔差集 | `TSolSubtract` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“布尔差集”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L136 | 布尔交集 | `TSolIntersect` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“布尔交集”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L137 | 编辑实体 | `TSolEdit` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“编辑实体”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L138 | 实体切割 | `TSolSlice` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“实体切割”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L139 | 分离最近 | `TSolSeparate` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“分离最近”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L140 | 完全分离 | `TSolSeparateAll` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“完全分离”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L141 | 去除参数 | `TSolPurge` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“去除参数”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L142 | 建筑立面 | `TBudElev` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“建筑立面”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L143 | 构件立面 | `TObjElev` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“构件立面”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L144 | 立面门窗 | `TEWinLib` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“立面门窗”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L145 | 门窗参数 | `TEWPara` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“门窗参数”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L146 | 立面窗套 | `elwct` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“立面窗套”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L147 | 立面阳台 | `TEBalLib` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“立面阳台”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L148 | 立面屋顶 | `tlmroof` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“立面屋顶”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L149 | 雨水管线 | `TEStrm` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“雨水管线”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L150 | 立面轮廓 | `TElevOutline` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“立面轮廓”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L151 | 图形裁剪 | `Tclip` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“图形裁剪”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L152 | 建筑剖面 | `TBudSect` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“建筑剖面”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L153 | 构件剖面 | `TObjSect` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“构件剖面”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L154 | 画剖面墙 | `sdwall` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“画剖面墙”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L155 | 双线楼板 | `sdfloor` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“双线楼板”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L156 | 预制楼板 | `sfloor1` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“预制楼板”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L157 | 加剖断梁 | `sbeam` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“加剖断梁”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L158 | 剖面门窗 | `TSectWin` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“剖面门窗”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L159 | 门窗过梁 | `MCGL` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“门窗过梁”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L160 | 剖面檐口 | `sroof` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“剖面檐口”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L161 | 参数楼梯 | `TSectStair` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“参数楼梯”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L162 | 参数栏杆 | `rltplib` | （总表已注册✓） | 必弹对话框（推断） | （推断，不是真机结论）中文名“参数栏杆”偏库、管理、设置、报表、导入导出、批量或参数配置语义，预测更像先进入对话框/面板。 |
| L163 | 楼梯栏杆 | `handrail` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“楼梯栏杆”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L164 | 楼梯拦板 | `handrail1` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“楼梯拦板”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L165 | 扶手接头 | `TConnectHandRail` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“扶手接头”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L166 | 剖面填充 | `FillSect` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“剖面填充”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L167 | 居中加粗 | `sltoplc2` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“居中加粗”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L168 | 向内加粗 | `sltopli2` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“向内加粗”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L169 | 取消加粗 | `pltosl` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“取消加粗”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L170 | 文字样式 | `TStyleEx` | （总表已注册✓） | 必弹对话框（推断） | （推断，不是真机结论）中文名“文字样式”偏库、管理、设置、报表、导入导出、批量或参数配置语义，预测更像先进入对话框/面板。 |
| L171 | 单行文字 | `TText` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“单行文字”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L172 | 多行文字 | `TMText` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“多行文字”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L173 | 转角自纠 | `TTextAdjust` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“转角自纠”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L174 | 文字转化 | `TTextConv` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“文字转化”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L175 | 文字合并 | `TTextMerge` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“文字合并”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L176 | 新建表格 | `TNewSheet` | （总表已注册✓） | 必弹对话框（推断） | （推断，不是真机结论）中文名“新建表格”偏库、管理、设置、报表、导入导出、批量或参数配置语义，预测更像先进入对话框/面板。 |
| L177 | 拆分表格 | `TSplitSheet` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“拆分表格”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L178 | 合并表格 | `TMergeSheet` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“合并表格”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L179 | 查找替换 | `TRepFind` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“查找替换”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L180 | 消除图元 | `TRemoveDupObj` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“消除图元”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L181 | 繁简转换 | `TBIG5_GB` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“繁简转换”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L182 | 统一字高 | `TEqualTextHeight` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“统一字高”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L183 | 专业词库 | `TWordLib` | （总表已注册✓） | 必弹对话框（推断） | （推断，不是真机结论）中文名“专业词库”偏库、管理、设置、报表、导入导出、批量或参数配置语义，预测更像先进入对话框/面板。 |
| L188 | 内门标注 | `TDimInDoor` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“内门标注”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L189 | 自由标注 | `TQuickDim` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“自由标注”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L191 | 快速标注 | `TQuickDim` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“快速标注”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L196 | 楼梯标注 | `TDIMSTAIR` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“楼梯标注”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L197 | 更改文字 | `TChDimText` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“更改文字”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L198 | 文字复位 | `TResetDimP` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“文字复位”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L199 | 文字复值 | `TResetDimT` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“文字复值”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L200 | 裁剪延伸 | `TDimTrimExt` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“裁剪延伸”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L201 | 取消尺寸 | `TDimDel` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“取消尺寸”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L202 | 连接尺寸 | `TMergeDim` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“连接尺寸”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L203 | 尺寸打断 | `TDimBreak` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“尺寸打断”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L204 | 增补尺寸 | `TBreakDim` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“增补尺寸”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L205 | 合并区间 | `TConbineDim` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“合并区间”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L206 | 拆分区间 | `TDimDivide` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“拆分区间”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L207 | 等分区间 | `TAverageDim` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“等分区间”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L208 | 对齐标注 | `TArrangeDim` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“对齐标注”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L209 | 切换角标 | `TDimTog` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“切换角标”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L210 | 尺寸转化 | `TConvDim` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“尺寸转化”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L211 | 尺寸自调 | `TDimAdjust` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“尺寸自调”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L212 | 设坐标系 | `wordcoord` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“设坐标系”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L214 | 坐标检查 | `TCheckCoord` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“坐标检查”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L216 | 标高对齐 | `TArrangeElev` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“标高对齐”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L219 | 索引图名 | `TIndexDim` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“索引图名”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L224 | 作法标注 | `TComposing` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“作法标注”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L228 | 通用图库 | `tkw` | （总表已注册✓） | 必弹对话框（推断） | （推断，不是真机结论）中文名“通用图库”偏库、管理、设置、报表、导入导出、批量或参数配置语义，预测更像先进入对话框/面板。 |
| L229 | 幻灯管理 | `TSlb` | （总表已注册✓） | 必弹对话框（推断） | （推断，不是真机结论）中文名“幻灯管理”偏库、管理、设置、报表、导入导出、批量或参数配置语义，预测更像先进入对话框/面板。 |
| L230 | 图块改层 | `TBlkLay` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“图块改层”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L231 | 图块替换 | `TRepIns` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“图块替换”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L232 | 生二维块 | `TMVBlock` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“生二维块”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L233 | 取二维块 | `TChMvBlock` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“取二维块”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L234 | 图块转化 | `TBlkConv` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“图块转化”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L235 | 矩形屏蔽 | `TBlkMask1` | （总表已注册✓） | 纯造点序列（推断） | （推断，不是真机结论）中文名“矩形屏蔽”是绘制/插入/布置类造物语义，预测可由位置点、两点或少量点列生成新实体；仍需确认是否有参数面板。 |
| L236 | 精确屏蔽 | `TBlkMask2` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“精确屏蔽”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L237 | 取消屏蔽 | `TBlkUnMask` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“取消屏蔽”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L238 | 图案加洞 | `THatchAddHole` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“图案加洞”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L239 | 图案减洞 | `THatchDelHole` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“图案减洞”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L240 | 任意屏蔽 | `WIPEOUT` | （总表已注册✓） | 纯造点序列（推断） | （推断，不是真机结论）中文名“任意屏蔽”是绘制/插入/布置类造物语义，预测可由位置点、两点或少量点列生成新实体；仍需确认是否有参数面板。 |
| L241 | 参照裁剪 | `XCLIP` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“参照裁剪”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L242 | 图案管理 | `TAW` | （总表已注册✓） | 必弹对话框（推断） | （推断，不是真机结论）中文名“图案管理”偏库、管理、设置、报表、导入导出、批量或参数配置语义，预测更像先进入对话框/面板。 |
| L243 | 木纹填充 | `MWTC` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“木纹填充”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L244 | 单元插图 | `TCellInsertBlock` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“单元插图”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L245 | 单元编辑 | `TCellEdit` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“单元编辑”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L246 | 单元递增 | `TCopyAndPlus` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“单元递增”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L247 | 单元累计 | `TTotalCellDigit` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“单元累计”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L248 | 单元合并 | `TCellMerge` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“单元合并”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L249 | 撤销合并 | `TDelMerge` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“撤销合并”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L250 | 线图案 | `TLinePattern` | （总表已注册✓） | 纯造点序列（推断） | （推断，不是真机结论）中文名“线图案”是绘制/插入/布置类造物语义，预测可由位置点、两点或少量点列生成新实体；仍需确认是否有参数面板。 |
| L251 | 当前比例 | `TPScale` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“当前比例”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L252 | 三维组合 | `TGroupFloor` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“三维组合”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L253 | 视口放大 | `TMaxVport` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“视口放大”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L254 | 视口恢复 | `TResVport` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“视口恢复”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L255 | 视图满屏 | `TFullView` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“视图满屏”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L256 | 视图存盘 | `TSaveView` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“视图存盘”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L257 | 设置立面 | `TUcsElev` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“设置立面”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L258 | 定位观察 | `TCamLocator` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“定位观察”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L259 | 关闭图层 | `TOffLayer` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“关闭图层”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L260 | 关闭其它 | `TOffOtherLayer` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“关闭其它”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L261 | 冻结图层 | `TFreezeLayer` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“冻结图层”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L262 | 冻结其它 | `TFreezeOtherLayer` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“冻结其它”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L263 | 锁定图层 | `TLockLayer` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“锁定图层”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L264 | 锁定其它 | `TLockOtherLayer` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“锁定其它”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L265 | 打开图层 | `TOnLayer` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“打开图层”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L266 | 解锁图层 | `TUnLockLayer` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“解锁图层”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L267 | 解冻图层 | `TUnFreezeLayer` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“解冻图层”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L268 | 图层全开 | `TopenAllLayer` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“图层全开”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L269 | 合并图层 | `TMergeLayer` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“合并图层”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L270 | 图层恢复 | `TResetAllLayer` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“图层恢复”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L271 | 图元改层 | `TGChangeLayer` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“图元改层”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L272 | 构件库 | `GJKW` | （总表已注册✓） | 必弹对话框（推断） | （推断，不是真机结论）中文名“构件库”偏库、管理、设置、报表、导入导出、批量或参数配置语义，预测更像先进入对话框/面板。 |
| L273 | 构件入库 | `InsertGJK` | （总表已注册✓） | 必弹对话框（推断） | （推断，不是真机结论）中文名“构件入库”偏库、管理、设置、报表、导入导出、批量或参数配置语义，预测更像先进入对话框/面板。 |
| L274 | 相机透视 | `TCamera` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“相机透视”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L275 | 视图固定 | `TFixView` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“视图固定”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L276 | 虚拟漫游 | `TVrCamera` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“虚拟漫游”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L277 | 环绕动画 | `TPathVideo` | （总表已注册✗） | 必弹对话框（推断） | （推断，不是真机结论）中文名“环绕动画”偏库、管理、设置、报表、导入导出、批量或参数配置语义，预测更像先进入对话框/面板。 |
| L278 | 穿梭动画 | `TWalkVideo` | （总表已注册✗） | 必弹对话框（推断） | （推断，不是真机结论）中文名“穿梭动画”偏库、管理、设置、报表、导入导出、批量或参数配置语义，预测更像先进入对话框/面板。 |
| L279 | 局部隐藏 | `TMkHide` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“局部隐藏”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L280 | 局部可见 | `TLocVis` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“局部可见”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L281 | 选择恢复 | `TUnHide` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“选择恢复”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L282 | 全部恢复 | `TUnHideAll` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“全部恢复”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L283 | 对象选择 | `TSelObj` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“对象选择”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L284 | 对象查询 | `TObjinfo` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“对象查询”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L285 | 对象编辑 | `TObjEdit` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“对象编辑”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L286 | 在位编辑 | `TInplaceEdit` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“在位编辑”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L287 | 图片填充 | `FillImage` | （总表已注册✗） | 必弹对话框（推断） | （推断，不是真机结论）中文名“图片填充”偏库、管理、设置、报表、导入导出、批量或参数配置语义，预测更像先进入对话框/面板。 |
| L288 | 颜色填充 | `FillColor` | （总表已注册✗） | 必弹对话框（推断） | （推断，不是真机结论）中文名“颜色填充”偏库、管理、设置、报表、导入导出、批量或参数配置语义，预测更像先进入对话框/面板。 |
| L289 | 材质附着 | `TMaterial` | （总表已注册✗） | 必弹对话框（推断） | （推断，不是真机结论）中文名“材质附着”偏库、管理、设置、报表、导入导出、批量或参数配置语义，预测更像先进入对话框/面板。 |
| L290 | 材质管理 | `TMatMan` | （总表已注册✗） | 必弹对话框（推断） | （推断，不是真机结论）中文名“材质管理”偏库、管理、设置、报表、导入导出、批量或参数配置语义，预测更像先进入对话框/面板。 |
| L291 | 简单渲染 | `TQuickRender` | （总表已注册✗） | 必弹对话框（推断） | （推断，不是真机结论）中文名“简单渲染”偏库、管理、设置、报表、导入导出、批量或参数配置语义，预测更像先进入对话框/面板。 |
| L292 | 自由复制 | `TDragCopy` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“自由复制”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L293 | 自由移动 | `TDragMove` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“自由移动”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L294 | 移位 | `TMove` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“移位”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L295 | 自由粘贴 | `TPasteClip` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“自由粘贴”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L296 | 布尔运算 | `TPolyBool` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“布尔运算”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L297 | 线变复线 | `TLineToPoly` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“线变复线”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L298 | 连接线段 | `TJointLine` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“连接线段”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L299 | 交点打断 | `tlinebk` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“交点打断”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L300 | 虚实变换 | `TChDash` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“虚实变换”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L301 | 加粗曲线 | `TPolyGlobleWidth` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“加粗曲线”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L302 | 消除重线 | `TRemoveDup` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“消除重线”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L303 | 前置 | `DrawOrderF` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“前置”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L304 | 后置 | `DrawOrderB` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“后置”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L305 | 置对象上 | `DrawOrderU` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“置对象上”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L306 | 置对象下 | `DrawOrderA` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“置对象下”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L307 | 水平齐左 | `ALXLEFT` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“水平齐左”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L308 | 水平齐右 | `ALXRIGHT` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“水平齐右”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L309 | 水平齐中 | `ALXMID` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“水平齐中”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L310 | 垂直齐顶 | `ALYTOP` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“垂直齐顶”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L311 | 垂直齐底 | `ALYBOTTOM` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“垂直齐底”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L312 | 垂直齐中 | `ALYMID` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“垂直齐中”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L313 | 水平间距 | `HoriDistribution` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“水平间距”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L314 | 垂直间距 | `VertDistribution` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“垂直间距”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L315 | 平行 | `ParallelEnt` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“平行”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L316 | 测量边界 | `TGetExtend` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“测量边界”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L317 | 统一标高 | `TModElev` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“统一标高”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L318 | 搜索轮廓 | `TSeOutline` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“搜索轮廓”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L319 | 图形裁剪 | `Tclip` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“图形裁剪”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L320 | 图形切割 | `TCutDrawing` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“图形切割”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L322 | 工程管理 | `TProjectManager` | （总表已注册✓） | 必弹对话框（推断） | （推断，不是真机结论）中文名“工程管理”偏库、管理、设置、报表、导入导出、批量或参数配置语义，预测更像先进入对话框/面板。 |
| L323 | 旧图转换 | `TConvTch` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“旧图转换”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L325 | 批量转旧 | `TBatSave` | （总表已注册✓） | 必弹对话框（推断） | （推断，不是真机结论）中文名“批量转旧”偏库、管理、设置、报表、导入导出、批量或参数配置语义，预测更像先进入对话框/面板。 |
| L326 | 图层转换 | `TLayerConvert` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“图层转换”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L328 | 图形变线 | `TMap2D` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“图形变线”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L329 | 图变单色 | `TMONO` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“图变单色”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L330 | 颜色恢复 | `TResColor` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“颜色恢复”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L331 | 图层管理 | `TLayerManager` | （总表已注册✓） | 必弹对话框（推断） | （推断，不是真机结论）中文名“图层管理”偏库、管理、设置、报表、导入导出、批量或参数配置语义，预测更像先进入对话框/面板。 |
| L332 | 定义视口 | `TMakeVPNew` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“定义视口”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L333 | 插入图框 | `TTitleFrame` | （总表已注册✓） | 必弹对话框（推断） | （推断，不是真机结论）中文名“插入图框”偏库、管理、设置、报表、导入导出、批量或参数配置语义，预测更像先进入对话框/面板。 |
| L334 | 图纸目录 | `TTitleList` | （总表已注册✓） | 必弹对话框（推断） | （推断，不是真机结论）中文名“图纸目录”偏库、管理、设置、报表、导入导出、批量或参数配置语义，预测更像先进入对话框/面板。 |
| L335 | 图纸保护 | `TProtect` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“图纸保护”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L336 | 改变比例 | `TChScale` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“改变比例”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L337 | 布局旋转 | `TLayoutRot` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“布局旋转”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L338 | 自定义 | `TCustomize` | （总表已注册✓） | 必弹对话框（推断） | （推断，不是真机结论）中文名“自定义”偏库、管理、设置、报表、导入导出、批量或参数配置语义，预测更像先进入对话框/面板。 |
| L339 | 日积月累 | `TCurTips` | （总表已注册✓） | 必弹对话框（推断） | （推断，不是真机结论）中文名“日积月累”偏库、管理、设置、报表、导入导出、批量或参数配置语义，预测更像先进入对话框/面板。 |
| L340 | 版本信息 | `TAbout` | （总表已注册✓） | 必弹对话框（推断） | （推断，不是真机结论）中文名“版本信息”偏库、管理、设置、报表、导入导出、批量或参数配置语义，预测更像先进入对话框/面板。 |
| L341 | 问题报告 | `TReportBug` | （总表已注册✓） | 必弹对话框（推断） | （推断，不是真机结论）中文名“问题报告”偏库、管理、设置、报表、导入导出、批量或参数配置语义，预测更像先进入对话框/面板。 |
| L342 | 长度统计 | `TLengthSumm` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“长度统计”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L343 | 踏步切换 | `TStepStoneSwitch` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“踏步切换”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L344 | 栏板切换 | `TRailingSwitch` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“栏板切换”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L345 | 幕墙转换 | `TConvertCurtain` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“幕墙转换”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L346 | 编号设置 | `TSetWinLab` | （总表已注册✓） | 必弹对话框（推断） | （推断，不是真机结论）中文名“编号设置”偏库、管理、设置、报表、导入导出、批量或参数配置语义，预测更像先进入对话框/面板。 |
| L347 | 图块改名 | `TBlkname` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“图块改名”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L349 | 门窗归整 | `TWDRegul` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“门窗归整”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L350 | 门窗填墙 | `TFillOpening` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“门窗填墙”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L351 | 尺寸等距 | `TDimISOmetry` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“尺寸等距”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L352 | 等式标注 | `TDivideDim` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“等式标注”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L354 | 备档拆图 | `bdct` | （总表已注册✓） | 必弹对话框（推断） | （推断，不是真机结论）中文名“备档拆图”偏库、管理、设置、报表、导入导出、批量或参数配置语义，预测更像先进入对话框/面板。 |
| L355 | 整图比对 | `tzbd` | （总表已注册✓） | 必弹对话框（推断） | （推断，不是真机结论）中文名“整图比对”偏库、管理、设置、报表、导入导出、批量或参数配置语义，预测更像先进入对话框/面板。 |
| L356 | 绘制梁 | `TGirDer` | （总表已注册✓） | 纯造点序列（推断） | （推断，不是真机结论）中文名“绘制梁”是绘制/插入/布置类造物语义，预测可由位置点、两点或少量点列生成新实体；仍需确认是否有参数面板。 |
| L357 | 碰撞检查 | `3wpz` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“碰撞检查”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L358 | 资源下载 | `TTgDownLoad` | （总表已注册✓） | 必弹对话框（推断） | （推断，不是真机结论）中文名“资源下载”偏库、管理、设置、报表、导入导出、批量或参数配置语义，预测更像先进入对话框/面板。 |
| L359 | 墙端封口 | `TChWallTerm` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“墙端封口”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L360 | 节能分析 | `openbec_` | （总表已注册✓） | 必弹对话框（推断） | （推断，不是真机结论）中文名“节能分析”偏库、管理、设置、报表、导入导出、批量或参数配置语义，预测更像先进入对话框/面板。 |
| L361 | 日照分析 | `opensun_` | （总表已注册✓） | 必弹对话框（推断） | （推断，不是真机结论）中文名“日照分析”偏库、管理、设置、报表、导入导出、批量或参数配置语义，预测更像先进入对话框/面板。 |
| L363 | BIM导入 | `TIMPORTXML` | （总表已注册✓） | 必弹对话框（推断） | （推断，不是真机结论）中文名“BIM导入”偏库、管理、设置、报表、导入导出、批量或参数配置语义，预测更像先进入对话框/面板。 |
| L364 | 转地形图 | `TConvertReliefmap` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“转地形图”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L365 | 场地红线 | `DrawRedLine` | （总表已注册✓） | 纯造点序列（推断） | （推断，不是真机结论）中文名“场地红线”是绘制/插入/布置类造物语义，预测可由位置点、两点或少量点列生成新实体；仍需确认是否有参数面板。 |
| L366 | 道路绘制 | `TRoad` | （总表已注册✓） | 纯造点序列（推断） | （推断，不是真机结论）中文名“道路绘制”是绘制/插入/布置类造物语义，预测可由位置点、两点或少量点列生成新实体；仍需确认是否有参数面板。 |
| L367 | 道路圆角 | `filroad` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“道路圆角”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L368 | 道路标高 | `TRoadElev` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“道路标高”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L369 | 道路坡度 | `TRoadGrade` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“道路坡度”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L370 | 道路半径 | `TArcRadDim` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“道路半径”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L371 | 路宽标注 | `TRoadWideDim` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“路宽标注”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L373 | 车位布置 | `TParkingSpace` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“车位布置”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L374 | 车位统计 | `TParkingStatistic` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“车位统计”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L375 | 删除车位 | `TParkingDelete` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“删除车位”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L376 | 成片布树 | `TMultTree` | （总表已注册✓） | 纯造点序列（推断） | （推断，不是真机结论）中文名“成片布树”是绘制/插入/布置类造物语义，预测可由位置点、两点或少量点列生成新实体；仍需确认是否有参数面板。 |
| L378 | 绘景观路 | `tlandscape` | （总表已注册✓） | 纯造点序列（推断） | （推断，不是真机结论）中文名“绘景观路”是绘制/插入/布置类造物语义，预测可由位置点、两点或少量点列生成新实体；仍需确认是否有参数面板。 |
| L379 | 总图标高 | `tzele` | （总表已注册✗） | 需选已有对象（推断） | （推断，不是真机结论）中文名“总图标高”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L380 | 坐标网格 | `TCOORDGRID` | （总表已注册✓） | 纯造点序列（推断） | （推断，不是真机结论）中文名“坐标网格”是绘制/插入/布置类造物语义，预测可由位置点、两点或少量点列生成新实体；仍需确认是否有参数面板。 |
| L381 | 风玫瑰 | `TWINDROSE` | （总表已注册✓） | 纯造点序列（推断） | （推断，不是真机结论）中文名“风玫瑰”是绘制/插入/布置类造物语义，预测可由位置点、两点或少量点列生成新实体；仍需确认是否有参数面板。 |
| L382 | 总平图例 | `TLayoutLegend` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“总平图例”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L383 | 单位设置 | `TUnitSet` | （总表已注册✓） | 必弹对话框（推断） | （推断，不是真机结论）中文名“单位设置”偏库、管理、设置、报表、导入导出、批量或参数配置语义，预测更像先进入对话框/面板。 |
| L384 | 建筑轮廓 | `TBuildOutLine` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“建筑轮廓”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L385 | 红线退让 | `TRedLineBack` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“红线退让”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L386 | 车位编号 | `TParkingNumber` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“车位编号”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L387 | 树名标注 | `TTreeNameLable` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“树名标注”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L388 | 树木统计 | `TTreeCount` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“树木统计”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L389 | 生坐标表 | `TCoordSheet` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“生坐标表”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L390 | 填充面积 | `THatchArea` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“填充面积”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L391 | 标注设置 | `TFireZoneSetting` | （总表已注册✓） | 必弹对话框（推断） | （推断，不是真机结论）中文名“标注设置”偏库、管理、设置、报表、导入导出、批量或参数配置语义，预测更像先进入对话框/面板。 |
| L392 | 分区创建 | `TFireZoneCreate` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“分区创建”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L393 | 面积修正 | `TFireZoneUpdata` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“面积修正”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L394 | 局部喷淋 | `TLocalSpray` | （总表已注册✓） | 纯造点序列（推断） | （推断，不是真机结论）中文名“局部喷淋”是绘制/插入/布置类造物语义，预测可由位置点、两点或少量点列生成新实体；仍需确认是否有参数面板。 |
| L395 | 楼板洞口 | `TAddHole` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“楼板洞口”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L396 | 疏散出口 | `TEvacuationExit` | （总表已注册✓） | 纯造点序列（推断） | （推断，不是真机结论）中文名“疏散出口”是绘制/插入/布置类造物语义，预测可由位置点、两点或少量点列生成新实体；仍需确认是否有参数面板。 |
| L397 | 房间路径 | `TSpaceEvacuatepath` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“房间路径”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L398 | 走道路径 | `TWalkWayEvacuatepath` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“走道路径”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L399 | 完全二维 | `TShow2D` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“完全二维”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L400 | 完全三维 | `TShow3D` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“完全三维”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L401 | 自动确定 | `TShowAuto` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“自动确定”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L402 | 显示全部 | `TShowAll` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“显示全部”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L403 | 编号翻转 | `TMirWinText` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“编号翻转”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L404 | 门 窗 库 | `TOpeningLib` | （总表已注册✓） | 必弹对话框（推断） | （推断，不是真机结论）中文名“门 窗 库”偏库、管理、设置、报表、导入导出、批量或参数配置语义，预测更像先进入对话框/面板。 |
| L405 | 屋面排水 | `TRoofOutLet` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“屋面排水”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L406 | 批量绑定 | `TBindReference` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“批量绑定”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L408 | 轴号组合 | `TLabelMerge` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“轴号组合”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L409 | 图块标注 | `tblockdim` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“图块标注”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L410 | 标注翻转 | `TMirDim` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“标注翻转”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L411 | 文字加框 | `TTextAddFrame` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“文字加框”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L412 | 文字互换 | `TTextExchange` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“文字互换”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L413 | 调字基点 | `TTextChangeAlign` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“调字基点”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L414 | 改块内字 | `TTextEditInBlock` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“改块内字”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L415 | 文字打断 | `TTextCut` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“文字打断”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L416 | 定位标注 | `TDimLoc` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“定位标注”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L417 | 曲线文字 | `TCurveText` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“曲线文字”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L418 | 房间分格 | `TDevideRoom` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“房间分格”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L419 | 轴号示意 | `TDriftLabel` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“轴号示意”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L420 | 属字转换 | `TTextAtrExc` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“属字转换”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L421 | 文字提取 | `TTextraText` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“文字提取”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L422 | 文字替换 | `TTextReplace` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“文字替换”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L423 | 转出Word | `Sheet2Word` | （总表已注册✓） | 必弹对话框（推断） | （推断，不是真机结论）中文名“转出Word”偏库、管理、设置、报表、导入导出、批量或参数配置语义，预测更像先进入对话框/面板。 |
| L424 | 读入Word | `Word2Sheet` | （总表已注册✓） | 必弹对话框（推断） | （推断，不是真机结论）中文名“读入Word”偏库、管理、设置、报表、导入导出、批量或参数配置语义，预测更像先进入对话框/面板。 |
| L425 | 转出Excel | `Sheet2Excel` | （总表已注册✓） | 必弹对话框（推断） | （推断，不是真机结论）中文名“转出Excel”偏库、管理、设置、报表、导入导出、批量或参数配置语义，预测更像先进入对话框/面板。 |
| L426 | 读入Excel | `Excel2Sheet` | （总表已注册✓） | 必弹对话框（推断） | （推断，不是真机结论）中文名“读入Excel”偏库、管理、设置、报表、导入导出、批量或参数配置语义，预测更像先进入对话框/面板。 |
| L427 | 转出WPS | `Sheet2WPS` | （总表已注册✓） | 必弹对话框（推断） | （推断，不是真机结论）中文名“转出WPS”偏库、管理、设置、报表、导入导出、批量或参数配置语义，预测更像先进入对话框/面板。 |
| L428 | 读入WPS | `WPS2Sheet` | （总表已注册✓） | 必弹对话框（推断） | （推断，不是真机结论）中文名“读入WPS”偏库、管理、设置、报表、导入导出、批量或参数配置语义，预测更像先进入对话框/面板。 |
| L429 | 合并标注 | `TMergemovedim` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“合并标注”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L430 | 标高改值 | `TChangeValElev` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“标高改值”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L431 | 加标注点 | `TAddLeaderPt` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“加标注点”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L432 | 删标注点 | `TDelLeaderPt` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“删标注点”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L434 | 符号对齐 | `TSymbAlign` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“符号对齐”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L435 | 改块基点 | `TBlkBasePt` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“改块基点”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L436 | 块内提取 | `TExtractEntFromBlock` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“块内提取”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L437 | 块内次序 | `TEntOrderInBlk` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“块内次序”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L438 | 快速建块 | `TBlkBuild` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“快速建块”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L439 | 开关切换 | `TSwitchAllLayer` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“开关切换”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L440 | 全部解冻 | `TUnfreezeAllLayer` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“全部解冻”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L441 | 删除轴线 | `TDelAxis` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“删除轴线”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L442 | 炸嵌套块 | `TExplodeBlock` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“炸嵌套块”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L443 | 图元改色 | `TPelChangeColor` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“图元改色”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L444 | 转化表格 | `TConverSheet` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“转化表格”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L445 | 字转房间 | `TTextToSpace` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“字转房间”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L446 | 修改边界 | `TModifySpaceEdge` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“修改边界”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L447 | 外参管理 | `TXREFLayerManager` | （总表已注册✓） | 必弹对话框（推断） | （推断，不是真机结论）中文名“外参管理”偏库、管理、设置、报表、导入导出、批量或参数配置语义，预测更像先进入对话框/面板。 |
| L448 | 单元清空 | `TClearCells` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“单元清空”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L449 | 单元拆分 | `TSplitCell` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“单元拆分”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L450 | 面积引注 | `TDimArea` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“面积引注”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L451 | 编号隐现 | `TOpingLabelVisible` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“编号隐现”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L452 | 辅助清理 | `TCleanHelper` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“辅助清理”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L453 | 图库改层 | `TBlkModifyLay` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“图库改层”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L454 | 多块改层 | `TMultipleBlockModifyLayer` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“多块改层”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L455 | 块内增减 | `TAddOrDelWithBlock` | （总表已注册✓） | 需选已有对象（推断） | （推断，不是真机结论）中文名“块内增减”语义依附已有轴网、墙、门窗、文字、尺寸、图块、边界或表格等对象，预测会命中选择对象步。 |
| L456 | 多块缩放 | `TBlocksScaling` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“多块缩放”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L457 | 多块旋转 | `TBlocksRotation` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“多块旋转”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L458 | 多块镜像 | `TBlocksMirror` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“多块镜像”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L459 | 快速布柜 | `TArrangeCabinets` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“快速布柜”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L460 | 绘制衣柜 | `TDrawTheWardrobe` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“绘制衣柜”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |
| L461 | 轮椅直径 | `TWheelchairDaim` | （总表已注册✓） | 纯造点序列（推断） | （推断，不是真机结论）中文名“轮椅直径”是绘制/插入/布置类造物语义，预测可由位置点、两点或少量点列生成新实体；仍需确认是否有参数面板。 |
| L462 | 连接检查 | `TCheckWallConnect` | （总表已注册✓） | 修改已有实体（推断） | （推断，不是真机结论）中文名“连接检查”偏显示切换、恢复、比例/图层/排序/状态变换或已有结果修改，预测不是新建实体流程。 |
| L463 | 带门隔断 | `TPartitionWithDoor` | （总表已注册✓） | 未知（推断） | （推断，不是真机结论）中文名“带门隔断”虽像构件/专业功能，但仅凭语义无法区分点序列、对象选择或参数面板，先不判为简单候选。 |

## 2. 排序候选短名单

只保留“预测为纯造点序列、单点或两三点即可生成新实体、无需先选已有对象、无需文字内联编辑”的候选。排序依据是离线语义上接近已封装官方命令 `TSymmetry` / `TRect` / `TBalcony` 的程度。

| 排名 | 命令名 | 中文名 | 注册 | 预测点序列 | 预测生成实体类型 | 风险点 |
|---:|---|---|---|---|---|---|
| 1 | `TLinePattern` | 线图案（推断） | （总表已注册✓） | 起点 -> 终点 -> 回车（推断） | 线图案或多段线类对象（推断） | 可能要求先选择已有曲线作为路径；若是“沿线生成”会命中墙1。（推断，不是真机结论） |
| 2 | `TBlkMask1` | 矩形屏蔽（推断） | （总表已注册✓） | 第一角点 -> 对角点（推断） | WIPEOUT/屏蔽对象（推断） | 可能要求先选被屏蔽图块或进入精确屏蔽设置。（推断，不是真机结论） |
| 3 | `WIPEOUT` | 任意屏蔽（推断） | （总表已注册✓） | 边界点列 -> 回车（推断） | WIPEOUT（推断） | 更像 AutoCAD 原生命令；实体增量可判但不是 TCH 智能实体。（推断，不是真机结论） |
| 4 | `TWheelchairDaim` | 轮椅直径（推断） | （总表已注册✓） | 中心点 -> 半径/方向点（推断） | 轮椅回转圆或无障碍符号（推断） | 命令名拼写为 Daim；也可能直接按标准直径单点插入。（推断，不是真机结论） |
| 5 | `TWINDROSE` | 风玫瑰（推断） | （总表已注册✓） | 插入点 -> 方向点（推断） | 风玫瑰符号/INSERT（推断） | 可能先弹风玫瑰参数/地区数据面板。（推断，不是真机结论） |
| 6 | `TRStair` | 双跑楼梯（推断） | （总表已注册✓） | 插入点 -> 回车（推断） | 楼梯类 TCH_* 实体（推断） | 楼梯族参数复杂，可能弹参数面板或需方向点。（推断，不是真机结论） |
| 7 | `tdrawautostair` | 自动扶梯（推断） | （总表已注册✓） | 插入点 -> 方向点/回车（推断） | 自动扶梯/楼梯类实体（推断） | lowercase 命令且可能有扶梯参数面板。（推断，不是真机结论） |
| 8 | `TDrawParallelStair` | 双分平行（推断） | （总表已注册✓） | 插入点 -> 回车（推断） | 楼梯类 TCH_* 实体（推断） | DrawStair 家族可能共用参数面板；退出语义未知。（推断，不是真机结论） |
| 9 | `TDrawCornerStair` | 双分转角（推断） | （总表已注册✓） | 插入点 -> 回车（推断） | 楼梯类 TCH_* 实体（推断） | 可能需要转角方向点或前置楼梯参数。（推断，不是真机结论） |
| 10 | `TDrawDoubleMulStair` | 双分三跑（推断） | （总表已注册✓） | 插入点 -> 回车（推断） | 楼梯类 TCH_* 实体（推断） | 多跑参数更多，面板风险高于双跑。（推断，不是真机结论） |
| 11 | `TDrawScissorsStair` | 交叉楼梯（推断） | （总表已注册✓） | 插入点 -> 回车（推断） | 楼梯类 TCH_* 实体（推断） | 复杂楼梯类型，可能先弹参数面板。（推断，不是真机结论） |
| 12 | `TDrawCrossStair` | 剪刀楼梯（推断） | （总表已注册✓） | 插入点 -> 回车（推断） | 楼梯类 TCH_* 实体（推断） | 复杂楼梯类型，可能先弹参数面板。（推断，不是真机结论） |
| 13 | `TDrawTriangleStair` | 三角楼梯（推断） | （总表已注册✓） | 插入点 -> 回车（推断） | 楼梯类 TCH_* 实体（推断） | 复杂楼梯类型，可能需要额外方向/角度输入。（推断，不是真机结论） |
| 14 | `TMultiStair` | 多跑楼梯（推断） | （总表已注册✓） | 插入点 -> 回车（推断） | 楼梯类 TCH_* 实体（推断） | 多跑路径可能不是单点，可能需要楼梯段/平台参数。（推断，不是真机结论） |
| 15 | `TGirDer` | 绘制梁（推断） | （总表已注册✓） | 起点 -> 终点 -> 回车（推断） | 梁构件或线性 TCH_* 实体（推断） | 可能先要求梁截面/标高参数，或需要依附墙柱轴线。（推断，不是真机结论） |
| 16 | `DrawRedLine` | 场地红线（推断） | （总表已注册✓） | 红线点列 -> 回车（推断） | 红线边界/多段线类对象（推断） | 可能需要闭合边界、编号或总图参数。（推断，不是真机结论） |
| 17 | `TCOORDGRID` | 坐标网格（推断） | （总表已注册✓） | 基点 -> 对角点/范围点 -> 回车（推断） | 坐标网格线/坐标标注（推断） | 网格间距/坐标系可能走面板或依赖当前坐标设置。（推断，不是真机结论） |
| 18 | `TEvacuationExit` | 疏散出口（推断） | （总表已注册✓） | 插入点 -> 方向点（推断） | 疏散出口符号（推断） | 消防模块可能要求分区/房间上下文。（推断，不是真机结论） |
| 19 | `TMultTree` | 成片布树（推断） | （总表已注册✓） | 范围两角点或边界点列 -> 回车（推断） | 多个树木 INSERT（推断） | 树种/密度可能弹库或参数面板。（推断，不是真机结论） |
| 20 | `TRoad` | 道路绘制（推断） | （总表已注册✓） | 起点 -> 下一点 -> 回车（推断） | 道路对象或多段线类对象（推断） | 道路宽度、圆角、标高等可能引入额外参数。（推断，不是真机结论） |
| 21 | `tlandscape` | 绘景观路（推断） | （总表已注册✓） | 起点 -> 下一点 -> 回车（推断） | 景观路对象或多段线类对象（推断） | 命令名泛化，可能依赖景观路参数面板。（推断，不是真机结论） |
| 22 | `TLocalSpray` | 局部喷淋（推断） | （总表已注册✓） | 范围两角点/局部边界 -> 回车（推断） | 喷淋点/喷头 INSERT（推断） | 可能需要房间、管网或喷淋间距设置。（推断，不是真机结论） |

## 3. 结论

- 剩余 407 行里，真正还值得真机一试的“简单”候选约 22 个（推断）：上表 1-5 是强候选，6-14 主要是楼梯族中候选，15-22 是场地/总图/消防/树木等低优先级候选。
- 如果按“像 `TSymmetry` / `TRect` 一样两三点就稳收尾，且大概率没有参数面板”这个更严口径，强候选只有 5 个（推断）：`TLinePattern`、`TBlkMask1`、`WIPEOUT`、`TWheelchairDaim`、`TWINDROSE`，且 `WIPEOUT` 可能不是 TCH 智能实体。
- 结论不是“完全没有了”，但高把握简单候选已经很少（推断）。大多数剩余命令从中文名看要么依附已有对象，容易命中“选择对象步不吃脚本点”墙1；要么偏管理/库/设置/导入导出，容易命中“模态对话框阻塞 IPC”墙2；其余未知项不应直接当简单封装入口。
- 本文没有写任何真机验证结论；所有命令名均来自 `docs/T20_OFFICIAL_COMMANDS.md`。
